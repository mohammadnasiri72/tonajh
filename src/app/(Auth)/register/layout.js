import React from "react";

export const metadata = {
  title: "ساخت حساب کاربری",
  description: "ساخت حساب کاربری",
};
export default async function layoutRegister({ children }) {
  return <div>{children}</div>;
}
